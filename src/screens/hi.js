import React from 'react';
import { StatusBar, View } from 'react-native';

const WelcomeScreen = (props)=>{

    return(
        <View style={{alignItems: 'center', justifyContent:'center', paddingTop: StatusBar.currentHeight}}>
        </View>
    )
}